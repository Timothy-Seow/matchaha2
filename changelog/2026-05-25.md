# 2026-05-25

## Done

- Created `CLAUDE.md` at project root as the entry-point context document for any agent picking up this project.
- Created `changelog/` directory; this is the first daily entry. Going forward, every meaningful change should append to the day's file here.
- Confirmed Supabase MCP is connected against project `qschgqucwolhhkplvcld`. URL + anon key are already in `js/supabase-client.js`.
- Applied `supabase/schema.sql` to project `qschgqucwolhhkplvcld` via MCP `apply_migration` (migration name `initial_schema`) after a `/mcp` reconnect picked up the edited `~/.claude.json` (no more `--read-only`). Verified via `list_tables`: all six tables (`profiles`, `products`, `orders`, `order_items`, `subscriptions`, `favourites`) present with RLS enabled; `products` seeded with 9 rows.
- Hardened `public.handle_new_user()` — `get_advisors` flagged that the `SECURITY DEFINER` trigger function was callable via `/rest/v1/rpc/handle_new_user` by anon/authenticated, which would let any caller insert a profile row for any uuid. Applied migration `restrict_handle_new_user_execute` to `revoke execute ... from public, anon, authenticated`. The trigger on `auth.users` still fires correctly because trigger execution doesn't require EXECUTE on the function for the calling role. Security advisor now clean.

## State of the project

- **Phase 1** (auth + empty account dashboard) is now end-to-end testable: code is written and DB schema is live with RLS.
- **Phase 2** (Stripe) and **Phase 3** (favourites / Apple Sign-In / password reset) not started.

## Next action

- Manual smoke test of Phase 1: `python -m http.server 8000` from project root → open `http://localhost:8000` → sign up → confirm landing on `account.html` and that a `profiles` row was created for the new user (`select * from public.profiles` via MCP).
- Reminder: email confirmation is on by default in Supabase. For local sign-up testing, disable it under Auth → Providers → Email → "Confirm email", or use the confirmation link Supabase emails.

## Notes

- Email confirmation is on by default in Supabase. For local sign-up testing, disable it under Auth → Providers → Email → "Confirm email".
- The plan at `~/.claude/plans/how-about-sign-in-nested-nest.md` has the full 3-phase breakdown; `SETUP.md` in the project root has the user-facing setup instructions.
