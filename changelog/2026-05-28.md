# 2026-05-28

## Done

- **Net change: none.** Attempted a header IA restructure (Home / Menu / Merchandise / Promotions / Reviews / About Us / Contact, with hover dropdowns for sub-items) based on a brief from the user. User rejected it ("revert back, not what i want") and the full change was reverted — all 7 HTML files (`index.html`, `shop.html`, `about.html`, `subscriptions.html`, `signin.html`, `signup.html`, `account.html`) and `css/styles.css` are back to pre-change state. Confirmed via curl that `index.html` still serves the original primary nav (Matcha / Ready to Drink / Subscription / Merchandise / Shop all) and secondary nav (Our Story / Our Cafés / A Tea Leaf Initiative).
- Started a local server (`python -m http.server 8000`) for manual testing.

## State of the project

- Identical to end of 2026-05-25. Phase 1 (auth + account shell) code + schema still in place; Phases 2 and 3 not started.

## Open / Pending

- The user has a new IA in mind for the header (the brief I tried to implement: Home, Menu / Drinks, Merchandise, Promotions, Reviews, About Us, Contact, with sub-items under each). Before re-attempting, **clarify what they actually want** — possibilities they hinted at or that came up:
  - Different visual treatment than the hover-dropdown pattern I used (mega menu? tabs? single flat row with no sub-items? off-canvas drawer?).
  - Different link targets / page structure (maybe they want the existing pages renamed/repurposed rather than 3 new pages stubbed).
  - Different brand framing (logo currently `matcha-haha`; brief mentioned `matcha.ha2`).
- Do NOT just re-apply the same 7-item-with-dropdowns markup — that was the version explicitly rejected.

## Notes

- Original Phase 1 smoke test (sign up → land on `account.html` → confirm `profiles` row) is still pending from 2026-05-25.
